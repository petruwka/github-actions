name: Github actions demo
on:
  push:
    branches: 
    - main
    paths-ignore:
    - ".github/**"
  pull_request:
    branches: 
    - main
  schedule:
    - cron: "15 6 * * 0"
  workflow_dispatch:
jobs:
  Build:
    runs-on: 
      labels: 
      - ubuntu-20.04
    steps:
    - uses: actions/checkout@v3.3.0
    - run: |
          echo ${{ github.event_name }}
          echo ${{ github.ref_name }}
          ls -ltr
  
          
          
      
